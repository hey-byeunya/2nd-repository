## 요약

평가 요약:

- 1. 완성된 코드 제출: ✅ — PRD 요구사항(목록/상세/장바구니, Supabase 연동, 익명 인증)이 대부분 구현됨
- 2. 설명의 이해도: ✅ — README "왜 이렇게 만들었나" 섹션과 코드 주석이 잘 되어 있음
- 3. 디버깅/새 시도 기록: ⬜ — `script.js`의 변수명 충돌 주석 정도가 유일한 사례, 더 필요
- 4. 회고: ⬜ — 레포 전체에서 회고 섹션을 찾지 못함
- 5. 코드 간결성: ✅ — 모듈화 잘 되어 있음, `image` 필드명 관련 소소한 개선 제안 포함

각 항목마다 실제 코드/README 인용을 근거로 첨부했습니다. 커밋: `208fb62`.

---

# AIFFEL Campus Code Peer Review Templete
- 코더 : 이정은
- 리뷰어 : 전대진

> 리뷰 대상: `dessert-shop-app/` (수제 디저트 쇼핑몰 — Supabase 연동 앱), 관련 기획 문서 `dessert-shop-prd/PRD.md`, 설계 문서 `backend-design/index.html` 포함

# PRT(Peer Review Template)

- [x]  **1. 주어진 문제를 해결하는 완성된 코드가 제출되었나요?**
- 문제에서 요구하는 기능이 정상적으로 작동하는지?
    - PRD(`dessert-shop-prd/PRD.md`)에서 이번 범위로 정의한 **상품 목록(카테고리 필터) → 상품 상세(품절 여부 표시) → 장바구니(수량 조절/삭제/합계)** 흐름이 `dessert-shop-app/script.js`에 모두 구현되어 있습니다.
        - 카테고리 필터: `renderProductGrid()`에서 `currentFilter`로 상품 목록을 필터링
        - 품절 표시/구매 차단: `openDetail()`에서 `addBtn.disabled = product.sold_out;`으로 품절 상품은 담기 버튼을 아예 비활성화
        - 장바구니 수량 조절·삭제·합계: `changeQty()`, `removeFromCart()`, `renderCart()`의 `total` 누적 계산
    - 목업 데이터 단계에서 그치지 않고 **Supabase 실 DB 연동**까지 완료했고, 익명 인증(`ensureSession()` → `signInAnonymously()`)으로 로그인 화면 없이도 사용자별 장바구니를 분리했습니다. `supabase/schema.sql`에 RLS 정책(`auth.uid() = user_id`)까지 걸어 "본인 장바구니만 접근 가능"을 DB 레벨에서 강제한 점이 인상적입니다.
    - README에 명시된 자체 검증 기준("상품 목록 → 상세 → 장바구니 담기 → 새로고침해도 장바구니가 그대로 남아있는지")도 `cart_items` 테이블에 영속되는 구조라 실제로 만족됩니다.
    - 다만 PRD 요구사항 6번("상품 상세 페이지가 검색 결과에 노출되어야 한다 — SEO 고려 서버 렌더링 필요")은 이번 구현이 순수 정적 사이트(CSR)라 충족되지 않습니다. 이 부분은 README의 "왜 이렇게 만들었나"에서 "PRD에서 추천한 Next.js는 SEO(SSR)가 필요해지는 다음 단계에서 다시 고려한다"고 스코프 제외를 명시적으로 밝혀두어, 누락이 아니라 **의도된 범위 조정**임을 알 수 있었습니다.

- [x]  **2. 핵심적이거나 복잡하고 이해하기 어려운 부분에 작성된 설명을 보고 해당 코드가 잘 이해되었나요?**
- 해당 코드 블럭에 doc string/annotation/markdown이 달려 있는지 확인
- 해당 코드가 무슨 기능을 하는지, 왜 그렇게 짜여진건지, 작동 메커니즘이 뭔지 기술.
- 주석을 보고 코드 이해가 잘 되었는지 확인
    - `dessert-shop-app/README.md`의 "왜 이렇게 만들었나" 섹션이 특히 잘 작성되어 있습니다.
        > "anon key를 코드에 그대로 넣는 이유: Supabase의 anon key는 원래 클라이언트(브라우저)에 노출되는 것을 전제로 설계된 값이며, 실제 방어선은 RLS 정책이다. 반드시 숨겨야 하는 값은 `service_role` key인데, 이 앱은 그 키를 아예 쓰지 않는다."
        
        단순히 "이렇게 했다"가 아니라 **왜 이 방식이 안전한지**까지 근거를 들어 설명하고 있어, 코드만 봐서는 의아할 수 있는 "anon key가 그대로 노출된 코드"의 설계 의도가 명확히 이해됐습니다.
    - `script.js` 최상단 주석도 실질적인 도움이 되었습니다.
        > "주의: 변수명을 `supabase`로 지으면 CDN 라이브러리가 이미 전역에 선언해둔 `window.supabase`(라이브러리 객체)와 이름이 충돌해 SyntaxError가 나므로 `supabaseClient`로 구분한다."
        
        코드를 처음 읽을 때 "왜 변수명이 `supabase`가 아니라 `supabaseClient`인가?"라는 의문이 바로 들 수 있는 지점인데, 그 이유를 정확히 짚어줘서 이해에 막힘이 없었습니다.
    - `supabase/schema.sql`에도 각 정책/함수 앞에 목적을 설명하는 주석이 달려 있어(`-- RLS 켜기만 하고 정책은 만들지 않음 → 기본적으로 전체 차단` 등) SQL을 몰라도 의도 파악이 가능했습니다.

- [ ]  **3. 에러가 난 부분을 디버깅하여 “문제를 해결한 기록”을 남겼나요? 또는 “새로운 시도 및 추가 실험”을 해봤나요?**
- 문제 원인 및 해결 과정을 잘 기록하였는지 확인
- 문제에서 요구하는 조건에 더해 추가적으로 수행한 나만의 시도, 실험이 기록되어 있는지 확인
    - 위 2번에서 언급한 `script.js`의 `supabase` 전역 변수명 충돌 주석이 사실상 이 항목에도 해당하는 좋은 사례입니다 — 실제로 겪었을 법한 에러(SyntaxError)의 원인과 해결책을 짧지만 정확하게 남겼습니다.
    - README의 "정적 사이트(Next.js 아님)" 섹션도 "PRD 추천안(Next.js) 대신 왜 정적 사이트로 갔는가"라는 **의사결정 기록**으로 볼 수 있습니다.
    - 다만 이 두 가지를 제외하면, 개발 과정에서 부딪혔을 다른 이슈들(예: Supabase Anonymous Sign-in 설정 과정에서의 시행착오, RLS 정책 작성 중 막혔던 부분, CORS/네트워크 이슈 등)에 대한 기록은 찾지 못했습니다. 커밋 로그(`89bce61 dessert-shop-prd PRD.md 추가` 등)도 결과물 추가 위주라 과정상의 문제 해결 기록으로 보기는 어려웠습니다.
    - **제안**: PR 설명이나 README에 "막혔던 부분 → 어떻게 풀었는지"를 짧게라도 별도 섹션으로 남기면, 지금 있는 두 개의 좋은 사례(변수명 충돌, Next.js 스코프 조정)가 더 잘 드러날 것 같습니다.

- [ ]  **4. 회고를 잘 작성했나요?**
- 프로젝트 결과물에 대해 배운점과 아쉬운점, 느낀점 등이 상세히 기록 되어 있나요?
	- 딥러닝 모델의 경우, 인풋이 들어가 최종적으로 아웃풋이 나오기까지의 전체 흐름을 도식화하여 모델 아키텍쳐에 대한 이해를 돕고 있는지 확인
    - `dessert-shop-app/`, `dessert-shop-prd/`, `backend-design/`, 저장소 전체를 확인했지만 배운 점/아쉬운 점/느낀 점을 담은 회고 섹션을 찾지 못했습니다. (이 과제는 딥러닝 모델이 아니므로 모델 아키텍처 도식화 항목은 해당하지 않습니다.)
    - **제안**: 예를 들어 "RLS 정책을 직접 설계해보며 배운 점", "Anonymous Sign-in을 선택하게 된 계기와 아쉬웠던 점", "다음에 Next.js로 SSR을 붙인다면 무엇이 바뀔지" 같은 내용을 README 하단이나 별도 `RETROSPECTIVE.md`로 남기면 좋을 것 같습니다.

- [x]  **5. 코드가 간결하고 효율적인가요?**
- 파이썬 스타일 가이드 (PEP8)를 준수하였는지 확인
- 코드 중복을 최소화하고 범용적으로 사용할 수 있도록 모듈화(함수화) 했는지
    - 이 프로젝트는 Python이 아니라 HTML/CSS/JavaScript(+ Supabase SQL)로 작성되어 PEP8은 해당하지 않지만, 동일한 취지로 코드 스타일 일관성을 확인했습니다. `const`/`async-await` 사용, 2-space 들여쓰기, 세미콜론 등 스타일이 파일 전체에서 일관됩니다.
    - 기능별 섹션 주석(`// ---------- 인증 ----------`, `// ---------- 장바구니 ----------` 등)으로 222줄짜리 `script.js` 안에서도 코드 위치를 빠르게 찾을 수 있게 모듈화되어 있습니다.
    - `el = (id) => document.getElementById(id)` 같은 짧은 헬퍼로 반복되는 DOM 조회 코드를 줄인 점, `formatWon()`으로 통화 포맷팅 로직을 한 곳에 모은 점도 좋습니다.
    - 백엔드 쪽에서도 `supabase/schema.sql`에서 RLS 정책으로 "본인 데이터만 접근 가능"을 강제해, 프론트엔드 코드에서 별도 권한 체크 로직을 짜지 않아도 되게 만든 설계가 효율적입니다.
    - **개선 여지**: `openDetail()`에서 `detail-desc`에 `product.image` 값을 그대로 넣고 있는데(`el("detail-desc").textContent = product.image;`), `image` 컬럼이 실제로는 사진이 아니라 상품 설명 텍스트를 담고 있어 필드명과 실제 용도가 어긋납니다. 지금은 이미지 자산이 없어 임시로 쓴 것으로 보이지만, `description` 같은 이름으로 바꾸면 이후에 실제 이미지 URL을 추가할 때 혼란을 줄일 수 있을 것 같습니다.

# 참고 링크 및 코드 개선

```
# 코드 리뷰 시 참고한 링크
- Supabase Row Level Security 공식 문서: https://supabase.com/docs/guides/database/postgres/row-level-security
  → cart_items 정책(auth.uid() = user_id)이 공식 권장 패턴과 일치하는지 확인하는 데 참고했습니다.

# 코드 리뷰를 통해 개선 제안한 부분
1. script.js의 `image` 필드 사용처(detail-desc)는 필드명을 `description`으로 분리하는 것을 제안 (5번 항목 참고)
2. 개발 중 겪은 문제/시행착오와 프로젝트 회고를 README 또는 별도 문서로 보강하는 것을 제안 (3, 4번 항목 참고)
```
